Выводы и навыки
После выполнения всех упражнений я овладел:

Базовой загрузкой и сохранением данных в pandas.

Работой с временными рядами (datetime, извлечение компонентов).

Категоризацией (pd.cut).

Предобработкой: удаление дубликатов, умная обработка пропусков.

Парсингом строк в новые колонки.

Фильтрацией и группировками с агрегатными функциями.

Обогащением данных через сэмплирование, генерацию и merge.

Созданием сводных таблиц (pivot_table).

Оптимизацией памяти и времени выполнения:

векторизация против циклов,

downcasting числовых типов,

категориальные типы,

